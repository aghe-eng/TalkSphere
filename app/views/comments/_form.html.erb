<%= simple_form_for([@category, @post, @post.comments.build]) do |f| %>
  <div class="form-group">
    <%= f.input :content, as: :rich_text_area, input_html: { class: "form-control" } %>
    <%= f.button :submit, "Add comment", class: "btn btn-primary mt-1" %>
  </div>
<% end %>
