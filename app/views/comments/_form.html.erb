
  <%= simple_form_for([@category, @post, comment], html: { multipart: true } ) do |f| %>
    <%= f.input :content, label: "Enter comment", as: :rich_text_area, input_html: { class: "form-control" } %>
    <%= f.button :submit, submit_label, class: "btn btn-primary mt-1" %>
  <% end %>
